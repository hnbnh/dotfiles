- Prefer using LSP tools when available.
- Use `rg` instead of `grep`.
- Use `fd` instead of `find`.

- For superpowers skills:
  - Always use `1. Subagent-Driven` option.
  - DO NOT commit plans and specs to repo.
